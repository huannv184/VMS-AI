# build & runtime
build/
.audit_runtime/
logs/
*.log

# data
data/
/database/

# dependencies
node_modules/

# env
.env
.jwt_secret

# IDE
.vscode/
.vs/
*.user
*.suo

# AI / media files
*.mp4
*.avi
*.pt
*.onnx
*.h5
# Inference engine binaries (per-GPU, must be regenerated locally — NEVER commit)
*.trt
*.engine
*.bin

# Windows debug symbols (large, regenerated by build)
*.pdb

# archive
*.zip
*.tar

# CMake / MSBuild generated artifacts (never commit these)
*.vcxproj
*.vcxproj.filters
*.sln
*.obj
cmake_install.cmake
CMakeCache.txt
CMakeFiles/
_deps/
CTestTestfile.cmake
RUN_TESTS.cmake
install_manifest.txt
vcpkg_installed/

# Frontend build output
vms-frontend/dist/

# OS / editor litter
.DS_Store

# External service binaries (large, downloaded at setup time via setup_infra.ps1)
external_services/postgresql/
external_services/mediamtx/
external_services/data/
external_services/minio.exe

# Claude Code settings (local only)
.claude/settings.local.json